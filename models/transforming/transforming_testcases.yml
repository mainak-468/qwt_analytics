version: 2
 
models:
  - name: trf_shipments
    columns:
      - name: status
        tests:
          - accepted_values:
              values: ['shipped', 'ordered', 'returned','replaced']

